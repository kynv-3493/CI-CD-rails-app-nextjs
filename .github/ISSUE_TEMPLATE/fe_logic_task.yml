name: 🧠 FE Logic Task
description: Call APIs, handle flows, integrate backend
title: "[FE]: "
labels: ["frontend", "logic", "task"]
assignees: []

body:
  - type: input
    id: goal
    attributes:
      label: "📄 Goal"
      placeholder: "Call login API, handle loading/error..."
    validations:
      required: true

  - type: textarea
    id: checklist
    attributes:
      label: "✅ Work checklist"
      description: "Write checklist in Markdown (each line should have `- [ ]`)"
      placeholder: |
        - [ ] Checklist 1
        - [ ] Checklist 2
        - [ ] Checklist 3

  - type: textarea
    id: related
    attributes:
      label: "📌 Related"
      placeholder: "API link, docs, PR..."
